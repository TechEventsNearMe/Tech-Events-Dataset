{
  "name": "TENM",
  "version": "1.0.0",
  "description": "Open dataset of tech events around the world.",
  "scripts": {
    "validate": "node scripts/validate.js",
    "build": "node scripts/build-events-json.js",
    "check": "npm run validate && npm run build",
    "update-readme": "node scripts/generate-readme-events.js"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "ajv-formats": "^3.0.1",
    "axios": "^1.13.2"
  }
}
